# Core runtime
pandas>=2.3.1
numpy>=2.2.6
regex>=2024.11.6
networkx>=3.4.2
tqdm>=4.67.1

# NLP + tagging stack
spacy>=3.8.7
sentence-splitter>=1.4
rank-bm25>=0.2.2 # keyword information retrieval
sentence-transformers>=5.0 # for the crossencoder/matching model. if you just want to use bm25 or another llm you won't need this

# OpenAI / async reasoning
openai>=1.99.9
tenacity>=9.1.2 # for request retries
pydantic>=2.12.3 # for the dynamic pydantic model in generate_answer in ornithologist-reasoning.py

# NOTE: Torch is an implicit dependency of sentence-transformers; install CPU or GPU build explicitly as needed:
#   pip install torch --extra-index-url https://download.pytorch.org/whl/cu121  (for CUDA 12.1 GPU)
# or let sentence-transformers pull a CPU wheel.

# Optional: if using half precision (FP16) on GPU ensure a compatible torch build.
